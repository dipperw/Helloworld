# Testgit Test1

Simple repository used for trying out git commands and quick Java experiments.

## Getting Started
- Clone the repo: `git clone <your-url>`
- Make changes under this directory.
- Use `git status`, `git add`, and `git commit` to track updates.

## Notes
- No build tooling is included yet; add what you need for your experiments.
- Keep this README updated with any custom steps or commands you rely on.
- 456